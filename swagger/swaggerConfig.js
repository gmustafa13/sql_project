const swaggerDefinition = {
    info: {
      title: 'testing swagger'
    },
    produces: [
      "application/json",
      "application/xml"
    ],
    schemes: ['http', 'https']
   
  };

module.exports=swaggerDefinition;